[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "visual-search"
version = "0.1.0"
description = "Visual search system with FastAPI backend and Streamlit UI"

[tool.setuptools.packages.find]
where = ["."]
